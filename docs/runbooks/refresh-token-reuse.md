# Refresh-token reuse

Treat the affected refresh family as compromised. Confirm the family and user through privacy-safe event IDs, verify all family sessions are revoked, and inspect adjacent login, IP/device and OAuth events. Notify the operator security contact, require fresh authentication, and rotate credentials when account takeover is plausible. Never reactivate the old family.
