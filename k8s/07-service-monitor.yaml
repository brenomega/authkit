apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: authkit
  labels:
    app: authkit
spec:
  selector:
    matchLabels:
      app: authkit
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s
