apiVersion: apps/v1
kind: Deployment
metadata:
  name: authkit
spec:
  replicas: 2
  selector:
    matchLabels:
      app: authkit
  template:
    metadata:
      labels:
        app: authkit
    spec:
      # Pod-level security hardening (DT 3.6.4)
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: authkit
          # CI/CD must replace this tag with the immutable digest of the promoted release.
          image: brenomega/authkit:0.0.1-SNAPSHOT
          # Container-level security hardening
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: authkit-secrets
            - configMapRef:
                name: authkit-network-config
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: jwt-keys
              mountPath: /etc/authkit/keys
              readOnly: true
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "1024Mi"
      volumes:
        - name: tmp
          emptyDir: {}
        - name: jwt-keys
          secret:
            secretName: authkit-jwt-keys
