apiVersion: v1
kind: Service
metadata:
  name: authkit-service
  labels:
    app: authkit
spec:
  selector:
    app: authkit
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
