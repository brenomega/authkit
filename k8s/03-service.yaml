apiVersion: v1
kind: Service
metadata:
  name: authkit-service
spec:
  selector:
    app: authkit
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  # Crucial for rate limiting: Preserves the client source IP rather than SNATing it through kube-proxy
  externalTrafficPolicy: Local
