# Unsupported systemd example

These units are host-integration examples only and are excluded from the v0.1 production path. They have no complete candidate proof for TLS, dependency isolation, secret loading, upgrades, restore, failure recovery, or sandbox compatibility. Use `deploy/golden` for the release-gated topology.
