/**
 * Declares infrastructure boundaries consumed by application services.
 * Implementations must preserve the atomicity, expiration, confidentiality, and
 * failure semantics documented on each SPI rather than merely matching signatures.
 */
package io.github.brenomega.authkit.service.spi;
