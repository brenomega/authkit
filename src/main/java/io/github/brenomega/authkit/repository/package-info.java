/**
 * Declares Spring Data persistence access. Derived CRUD queries retain their
 * framework semantics; explicit documentation is reserved for conditional writes,
 * locking, tenant scoping, and ordering that carries a domain guarantee.
 */
package io.github.brenomega.authkit.repository;
