/**
 * Defines OAuth provider state for clients, consent, and authorization codes.
 * OAuth identities and grants may be tenant-bound or global as explicitly recorded
 * on the client; a missing tenant does not imply the caller's tenant.
 */
package io.github.brenomega.authkit.domain.oauth;
