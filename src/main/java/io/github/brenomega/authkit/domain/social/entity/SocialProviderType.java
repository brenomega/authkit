package io.github.brenomega.authkit.domain.social.entity;

/** Identifies provider-specific policy layered over the common OIDC protocol. */
public enum SocialProviderType {
    GOOGLE,
    GENERIC_OIDC,
    GOVBR_EXPERIMENTAL
}
