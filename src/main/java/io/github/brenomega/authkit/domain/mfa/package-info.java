/**
 * Defines TOTP credentials, one-time backup codes, login challenges, and their
 * boundary types. Raw TOTP secrets and backup codes are transient outputs; stored
 * representations are encrypted or keyed digests.
 */
package io.github.brenomega.authkit.domain.mfa;
